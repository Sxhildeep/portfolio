/** Shared load timing so curtain + hero intro stay in sync. */
export const CURTAIN_DELAY = 0.2;
export const CURTAIN_DURATION = 1.1;

/** Hero starts rising as the curtain clears the midpoint. */
export const HERO_INTRO_DELAY = CURTAIN_DELAY + CURTAIN_DURATION * 0.45;
